# Optional: analytics / API overrides
# VITE_CONTACT_API=
# VITE_ANALYTICS_ID=
#
# Contact form email: when deployed with parent repo (kiryuchi10.github.io) on Netlify,
# the contact form POSTs to /api/contact. Set these in Netlify UI (not here) for sending:
#   SENDER_EMAIL, SENDER_PASSWORD, RECIPIENT_EMAIL
# See kiryuchi10.github.io/netlify/functions/contact.js
